#include "civic.h"

export int main() {
    int p = 5;
    int c = (int) (5.0);
    int a = c + (int) true;
    int d = (int) true;
    float e = (float) false;
    int f = (int) (c > p);
    printInt(p);
    printInt(a);
    printInt(f);
    return 0;
}
