#include "civic.h"

export int main() {
    for(int i=0, 5) {
        for(int j=0, 4) {
            printInt(i);
            printSpaces(1);
            printInt(j);
            printSpaces(1);
        }

        printNewlines(1);
    }
    return 0;
}
